# v0.2 capabilities: zero authority by default, scoped grant, non-fatal probe.
`s hv .                              # 0  -> no authority to start
`s [ `s rq $send-ok$ . `s hv . ] gr  # grant `s for this block
`s hv .                              # 0  -> authority restored after the block
